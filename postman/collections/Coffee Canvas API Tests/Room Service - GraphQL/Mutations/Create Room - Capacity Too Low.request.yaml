$kind: http-request
name: 'Create Room – Capacity Too Low'
method: POST
url: 'http://localhost:3002/graphql'
order: 2000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {"query": "mutation CreateRoom($input: CreateRoomInput!) { createRoom(input: $input) { token room { id } } }", "variables": { "input": { "capacity": 0 } }}
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200", () => pm.response.to.have.status(200));
      pm.test("Returns GraphQL error for invalid capacity", () => {
        const json = pm.response.json();
        pm.expect(json.errors).to.be.an('array').and.not.empty;
      });
